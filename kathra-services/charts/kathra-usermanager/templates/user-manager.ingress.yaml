apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: usermanager
  annotations:
    kubernetes.io/ingress.class: traefik
  labels:
{{ include "kathra.labels" . | indent 4 }}
    ingress: "{{ if .Values.tls }}tls{{ else }}plain{{ end }}"
spec:
  rules:
  - host: "usermanager.{{ .Values.global.domain }}"
    http:
      paths:
      - backend:
          serviceName: usermanager
          servicePort: 80
