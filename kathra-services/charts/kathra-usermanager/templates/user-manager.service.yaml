apiVersion: v1
kind: Service
metadata:
  name: usermanager
  labels:
{{ include "kathra.labels" . | indent 4 }}
    app: usermanager
    product: kathra
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: usermanager
    product: kathra
  type: ClusterIP
