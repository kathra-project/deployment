apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: binaryrepositorymanager-harbor
  annotations:
    kubernetes.io/ingress.class: traefik
  labels:
{{ include "kathra.labels" . | indent 4 }}
    ingress: "{{ if .Values.tls }}tls{{ else }}plain{{ end }}"
spec:
  rules:
  - host: "binaryrepositorymanager-harbor.{{ .Values.global.domain }}"
    http:
      paths:
      - backend:
          serviceName: binaryrepositorymanager-harbor
          servicePort: 80
