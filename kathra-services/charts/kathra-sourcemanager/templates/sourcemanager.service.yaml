apiVersion: v1
kind: Service
metadata:
  name: sourcemanager
  labels:
{{ include "kathra.labels" . | indent 4 }}
    app: sourcemanager
    product: kathra
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: sourcemanager
    product: kathra
  type: ClusterIP
