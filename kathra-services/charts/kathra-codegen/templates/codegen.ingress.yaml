apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: codegen
  annotations:
    kubernetes.io/ingress.class: traefik
  labels:
{{ include "kathra.labels" . | indent 4 }}
    ingress: "{{ if .Values.tls }}tls{{ else }}plain{{ end }}"
spec:
  rules:
  - host: "codegen.{{ .Values.global.domain }}"
    http:
      paths:
      - backend:
          serviceName: codegen
          servicePort: 80
