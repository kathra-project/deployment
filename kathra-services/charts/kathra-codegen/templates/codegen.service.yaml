apiVersion: v1
kind: Service
metadata:
  name: codegen
  labels:
{{ include "kathra.labels" . | indent 4 }}
    app: codegen
    product: kathra
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: codegen
    product: kathra
  type: ClusterIP