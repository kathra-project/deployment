apiVersion: v1
kind: ServiceAccount
metadata:
  name: catalogmanager
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "kathra.labels" . | indent 4 }}
---
kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: catalogmanager-role
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "kathra.labels" . | indent 4 }}
rules:
- apiGroups:
  - '*'
  resources:
  - 'configmaps'
  verbs: ["get","list"]
---
kind: RoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: catalogmanager-rolebinding
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "kathra.labels" . | indent 4 }}
subjects:
- kind: ServiceAccount
  name: catalogmanager
  namespace: {{ .Release.Namespace }}
roleRef:
  kind: Role
  name: catalogmanager-role
  apiGroup: rbac.authorization.k8s.io

