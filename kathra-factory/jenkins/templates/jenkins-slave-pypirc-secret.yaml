apiVersion: v1
kind: Secret
metadata:
  name: pypi-config
  labels:
    app: {{ template "jenkins.fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
type: Opaque
data:
  .pypirc: {{ include "pypirc" . | b64enc }}